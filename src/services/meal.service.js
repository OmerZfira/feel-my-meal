import Vue from 'vue';

function submitMeal( {email,password} ) {
//   return Vue.http.post('http://localhost:3003/login', {username: email, pass: password} )
//     .then(res => res.json())
//     .then(({token, user}) => {
//       console.log('Signedin user:', user);
//       setSession(token, user);
//       return user;
    // })


}